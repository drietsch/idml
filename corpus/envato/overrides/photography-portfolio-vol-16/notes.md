# photography-portfolio-vol-16

Image-heavy portfolio layout, minimal text. Primary value of the
pack as a fidelity fixture is the image pipeline (clipped fills,
fitted frames, EPS/PNG/JPG link resolution).

Declared fonts: Inter only. We ship Inter, so the textual diff
should be near-zero; image fidelity dominates.

Candidate for promotion to `stage: gated` once we've measured a
baseline ΔE / SSIM and chosen thresholds.
